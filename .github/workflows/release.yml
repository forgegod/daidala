name: release-gate

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install development dependencies
        run: python -m pip install --upgrade pip && python -m pip install -e '.[dev]'
      - name: Verify source and documentation
        run: |
          python scripts/check_release_contents.py .
          python scripts/check_md_links.py .
          ruff check .
          pytest
          daidala packs validate addyosmani
          daidala packs validate aidlc

  package:
    name: Package and dependency audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Build distributions
        run: |
          python -m pip install --upgrade pip
          python -m pip install build twine pip-audit
          python -m build
          python -m twine check dist/*
      - name: Inspect release contents
        run: python scripts/check_release_contents.py . --wheel dist/*.whl
      - name: Isolated wheel install and runtime audit
        run: |
          python -m venv /tmp/daidala-release
          /tmp/daidala-release/bin/pip install --upgrade pip
          /tmp/daidala-release/bin/pip install dist/*.whl pip-audit
          /tmp/daidala-release/bin/daidala packs validate addyosmani
          /tmp/daidala-release/bin/daidala packs validate aidlc
          /tmp/daidala-release/bin/pip-audit --local

  hermes-compatibility:
    name: Supported Hermes compatibility
    if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/v')
    needs: [test, package]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
      - name: Install pinned Hermes and Daidala
        run: |
          git clone https://github.com/NousResearch/hermes-agent.git /tmp/hermes-agent
          git -C /tmp/hermes-agent checkout 4281151ae859241351ba14d8c7682dc67ff4c126
          git -C /tmp/hermes-agent update-ref refs/remotes/origin/main 4281151ae859241351ba14d8c7682dc67ff4c126
          git -C /tmp/hermes-agent remote set-url origin file:///tmp/hermes-agent-pinned-origin
          (cd /tmp/hermes-agent && npm ci --workspace web && npm run build -w web)
          python -m pip install --upgrade pip
          python -m pip install -e /tmp/hermes-agent
          python -m pip install -e .
      - name: Probe supported Hermes boundary
        run: |
          python scripts/probe_hermes_compatibility.py
          python scripts/probe_hermes_dashboard_compatibility.py
